# Wiring

INMP441: WS→GPIO25, SCK→GPIO26, SD→GPIO33, VDD→3.3V, GND→GND. Capture button: GPIO14→GND. Status LED: GPIO2→220Ω→LED→GND.

The public build is a browser digital twin. Firmware and wiring are implementation-ready but not physically validated.
